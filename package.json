{
  "name": "portofolio",
  "version": "1.0.0",
  "description": "Portfolio website with Three.js oak tree growth concept",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "gsap": "^3.14.2",
    "three": "^0.183.2"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.27.0",
    "vite": "^7.3.1",
    "wrangler": "^4.72.0"
  }
}
